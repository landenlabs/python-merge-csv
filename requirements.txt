# Main data processing stack
# Use pip to install dependencies
#    python -m pip install -r requirements.txt --dry-run
#    python -m pip install -r requirements.txt

# NumPy 2.x causes silent crashes on this Windows build; stay on 1.x
numpy>=1.26.4,<2.0; sys_platform == 'win32'  # Force version 1.x for stability

# Pandas 3.x is compatible with NumPy 1.26.4
pandas>=3.0.1
